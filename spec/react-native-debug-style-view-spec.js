'use babel';

import ReactNativeDebugStyleView from '../lib/react-native-debug-style-view';

describe('ReactNativeDebugStyleView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
